# فواتير — خارطة الطريق التجارية وسِجلّ التقدّم

> **هذه ترجمة عربية لـ [`ROADMAP.md`](ROADMAP.md).** النسخة الإنجليزية هي المرجع
> الأساسي؛ عند أي اختلاف بينهما، عُد إليها. أسماء الملفات والأصناف والدوال
> ومعرّفات الـ commits تُركت بالإنجليزية عمداً لأنها تطابق ما في الشيفرة.

تحويل **فواتير** من تطبيق نقاط بيع (POS) عادي يعمل دون إنترنت إلى منتج تجاري
يحمل **اشتراكات مُتحقَّقاً منها عبر الإنترنت** و**دفتر ديون للزبائن**.

**الفرع:** `refactor/architecture-hardening`
**إصدار التطبيق:** `1.0.1+1` · **مخطط Drift:** **v15**
**آخر تحديث:** 2026-07-27 (جولة تحقّق ميدانية، والخادم الخاص يعمل على
`evotech-sys.com`)

> **أين يقف المشروع: فواتير في استخدام إنتاجي حقيقي.** يُدير محلّ صديق يومياً،
> وقد تحقّق المالك من كل ميزة على هاتفه فور شحنها — مُبلِّغاً عن أي خلل فوراً،
> وهو ما وُلدت منه معظم الخطة 011. لم يعد هذا برنامجاً قبل الإطلاق.
>
> قائمة الميزات **خالية من أي شيء تمّ الالتزام به**. عشرٌ من الخطط المرقّمة
> الاثنتَي عشرة قد شُحنت؛ والخطتان اللتان لم تُنفَّذا (002 المزامنة متعددة
> الأجهزة، و009 المساعد الذكي) **أُجّلتا عن قصد إلى الإصدار الثاني**، بينما
> **رُفضت** الخطة 004 نهائياً. لا يوجد شيء نصف مبنيّ.
>
> **والتطبيق يعمل الآن على خادمه الخاص** (`api.evotech-sys.com`) — فقد تقاعد
> خادم Smart-Agent المؤقت، وتقاعدت معه استضافة الإعدادات الهشّة على Google Drive.
> انظر [التحقّق الميداني](#التحقّق-الميداني--ما-الذي-يعمل-في-محل-حقيقي) لمعرفة ما
> تأكّد على الأجهزة، والشيء الوحيد الذي لم يتأكّد بعد.

---

## قرارات مقفلة

- **التفعيل يقوده المشغّل.** يرفع التطبيق طلباً بحالة `status:'pending'`، ثم
  يُحوّل المستخدم إلى واتساب/تيليغرام، ويقوم إنسان بتفعيل الجهاز من جهة الخادم
  (لا مدفوعات آلية داخل التطبيق).
- ~~**الخادم = إعادة استخدام خادم Smart-Agent**~~ ← **تجاوزَه الواقع. فواتير
  يعمل على منصّة EVOTECH المملوكة** (`evotech-core`)، بعنوان أساسي
  `https://api.evotech-sys.com/api/fawateer`، وما يزال بمفتاح
  `app_name: 'Fawateer'`. بُنيت المنصّة لتطابق الحمولات القائمة تماماً
  (`create_device`/`check_device`/`update_my_data`/`getPlans`/`add_review`)، لذا
  **لم يتغيّر شيء في ميزة الترخيص** — أي أن قرار الحل المؤقت أثمر كما صُمِّم له.
  ومقطع `/api/fawateer` **حامل للحمل** (لا `/api` مجرّداً): فـ `getPlans` لا
  يحمل `app_name`، فالعنوان هو الشيء الوحيد الذي يُخبر المنصّة أي قائمة أسعار
  تُعيد.
- **المبالغ تبقى `double`** في كل التطبيق (لا هجرة إلى وحدات صحيحة صغرى)؛
  تُقرَّب عند العرض أو عند الكتابة في الدفتر.
- **ترتيب البناء:** الترخيص أولاً، ثم دفتر الديون. *(كلاهما مُنجَز.)*

### مقفلة لاحقاً — أعراف التزمت بها كل خطة

ظهرت هذه أثناء البناء وصارت الآن حاملة للأحمال عبر كل الميزات. كسر أي واحدة
منها تغييرٌ شامل، لا تغييرٌ محلي.

- **التقاط لقطة وقت البيع؛ التاريخ غير قابل للتعديل.** يُجمّد `sales_items` كلاً
  من `price` و`cost` و`fxRate` و`priceOriginal` و`discount` و`attributesSnapshot`
  و`saleType`. إعادة طباعة فاتورة عمرها ستة أشهر يجب أن تطابق الأصل حرفاً بحرف.
  الخطة 004 موجودة للدفاع عن هذا المبدأ.
- **تُحفظ التعدادات بالاسم، لا بالفهرس** (`ProductSaleType`، `PriceCurrency`،
  `CashTransactionType`، أنواع قيود الدفتر، `AppFontScale`) — كي لا تؤدي إعادة
  ترتيب الحالات إلى إعادة تعيين صفوف قائمة.
- **الهجرات تراكمية.** أضِف كتلة `if (from < N)` جديدة؛ لا تُعدّل كتلة سبق
  شحنها. لا تزال هجرة v5→v6 هي **إعادة البناء الوحيدة** لجدول في تاريخ التطبيق.
- **فكّر بجدول `AppSettings` (مفتاح/قيمة) قبل إضافة جدول جديد.** عدة ميزات شُحنت
  بلا أي هجرة لأن حالتها سطران من نوع مفتاح-قيمة.
- **التجميعات تُحسب في SQL**، لا بتحميل الصفوف إلى Dart وجمعها.
- **لا نصوص إنجليزية تظهر للمستخدم داخل الـ BLoC** — خزّن خطأً مُصنَّفاً، ودع
  الصفحة تربطه بنص من ملفات ARB.
- **الاختبارات تستخدم بدائل (fakes) مكتوبة يدوياً**، لا Drift أو الإضافات
  الحقيقية؛ و`integration_test/` هو الاستثناء الضيّق حين يكون المحرك الأصلي نفسه
  هو موضع الاختبار.

### مشاريع مرجعية
- الترخيص/معمارية الخادم ← `D:\work\Flutter\Smart-Agent` (`smart_agent`).
- دفتر الديون ← `D:\work\Flutter\AccountingBook\accounting_book`.

---

## التقدّم بلمحة

| المرحلة | الميزة | الحالة |
|-------|---------|--------|
| 0–1 | نواة الشبكة + بوابة الترخيص | ✅ منجز |
| 1 | كشف الحساب — مشاركة | ✅ منجز |
| 1 | شاشة إدارة الاشتراك | ✅ منجز |
| 1 | عرض معرّف الجهاز | ✅ منجز |
| 2 | دفتر الديون (زبائن + بيع بالأجل) | ✅ منجز |
| 2 | تبويب الدفتر في الشريط السفلي | ✅ منجز |
| 2 | كشف الحساب — طباعة حرارية | ✅ منجز |
| 3 | فكّ القفل الفوري عبر FCM | ✅ منجز — **مُتحقَّق منه على جهاز** |
| 3 | معرّف الجهاز على iOS | ✅ منجز |
| 3 | شريط الإشعار أثناء عمل التطبيق | ✅ منجز |
| 3.5 | الإعدادات البعيدة (عنوان الخادم + الدعم) + فحص التحديث | ✅ منجز — مستضاف ذاتياً |
| 3.5 | عنوان الـ API وقت التشغيل + إصلاح تجمّد شاشة البدء | ✅ منجز |
| 3.5 | تعديل بيانات الحساب (الاسم/الهاتف) في الإعدادات | ✅ منجز |
| POS | منتقي المنتجات — قائمة حيّة + تغذية راجعة عند الإضافة | ✅ منجز |
| POS | البيع بالوزن / نوع البيع (مخطط **v8**) | ✅ منجز |
| POS | **مركز تدقيق** المبيعات (فلترة/ملخّص/ترقيم صفحات) | ✅ منجز |
| النقد | **الصندوق** / درج النقد (مخطط **v9**) | ✅ منجز |
| الدفتر | منع تكرار اسم الزبون | ✅ منجز |
| البناء | إصلاح بناء الإصدار على أندرويد (`flutter_vibrate` / lStar) | ✅ منجز |
| البناء | توقيع الإصدار بمفتاح مملوك + ملفات APK لكل معمارية | ✅ منجز |
| الهوية | أيقونة الشريط-ف التكيّفية، وخط Cairo مضمّن دون إنترنت | ✅ منجز |
| POS | وضع المخزون الصارم (منع البيع تحت الصفر — اختياري) | ✅ منجز |
| 4+ | المشتريات / المورّدون / المصاريف / تعدد الفروع | ⬜ مستقبلاً |

### الخطط المرقّمة (`docs/plans/`)

وثائق التصميم هي مرجع المبررات — تعليقات الشيفرة تشير إليها بالرقم. ويحمل ترويسة
كل خطة تفاصيلها كما بُنيت فعلاً.

| الخطة | الميزة | الحالة |
|---|---|---|
| **001** | النسخ الاحتياطي والاستعادة السحابية (Google Drive) | ✅ مشحونة — نطاق `drive.file`، و`VACUUM INTO`، ونسخ تلقائي. **الاستعادة مُتحقَّق منها على جهاز** |
| **002** | المزامنة متعددة الأجهزة | ⏸️ **مؤجّلة للإصدار الثاني** — لا شيفرة. أصعب بند في خارطة الطريق |
| **003** | العملة المزدوجة (ل.س أساساً + تسعير بالدولار) | ✅ مشحونة — مخطط **v10** |
| **004** | إعادة احتساب الأسعار التاريخية | ❌ **مرفوضة نهائياً.** ليست بنداً مؤجّلاً — التاريخ غير قابل للتعديل |
| **005** | العروض / الخصومات | ✅ الإصدار الأول مشحون — مخطط **v12**. محرّك القواعد وسعر التخفيض الدائم مؤجّلان |
| **006** | الفترة التجريبية المجانية | ✅ مشحونة — راية عرض واحدة، بلا حالة جديدة وبلا هجرة |
| **007** | التكامل مع واتساب والمشاركة (بطاقات PNG) | ✅ مشحونة — `core/share/`. توليد PDF مؤجّل |
| **008** | لوحة التحليلات | ✅ إصدار أول مُبسّط مشحون — بلا جداول جديدة. القائمة v1.5 مؤجّلة |
| **009** | المساعد الذكي والتنبيهات | ⏸️ **مؤجّلة للإصدار الثاني** — لا شيفرة. الخطة 008 تشحن النسخة السلبية منها |
| **010** | خصائص المنتجات الديناميكية | ✅ السلة A **مكتملة** (v1→v1.4)، مخطط **v13**. السلّتان B/C خطط منفصلة |
| **011** | إصلاحات ملاحظات الميدان للإصدار الأول (11 بنداً) | ✅ **الأحد عشر كلها مشحونة** + متابعتان، مخطط **v14** |
| **012** | القطع المُسلسَلة (IMEI / الرقم التسلسلي) | ✅ الإصدار الأول مشحون — مخطط **v15**. السلة C من الخطة 010؛ الهجرة لم تُتحقّق على جهاز بعد |

---

## المنجَز — بالتفصيل

### ✅ المرحلة 0+1 — نواة الشبكة + بوابة الترخيص
- `core/network/` (`ApiClient`، `ApiConfig`) — أول اتصال للتطبيق بخادم.
- `features/licensing/` — تفعيل بمعرّف الجهاز (`SHA-256(ANDROID_ID + salt)`)،
  ونداءات `create_device` / `check_device` / `getPlans`، وتخزين `expires_at` في
  SharedPreferences، مع حارسَي **مهلة عمل دون إنترنت** و**التلاعب بالساعة**
  (`LicenseGuards` النقي).
- **البوابة** في GoRouter: غير مرخَّص ← `/activation`، مفعّل ← واجهة التبويبات،
  قبل الإقلاع ← `/splash`. تعتمد على `LicenseState.bootstrapped` (لا على
  `checking` العابرة).
- أخطاء جديدة: `NetworkFailure`، `ServerFailure`.
- الاعتماديات: http، crypto، android_id، shared_preferences، url_launcher.

### ✅ شاشة إدارة الاشتراك
- الإعدادات ← **الاشتراك** (`SubscriptionStatusPage`، `/settings/subscription`):
  الحالة / الباقة / الانتهاء / الأيام المتبقية / آخر مزامنة + **تحديث** +
  **تجديد**.

### ✅ عرض معرّف الجهاز
- `DeviceIdCard` (نسخ بلمسة) في شاشتَي التفعيل والاشتراك؛ ويُحمَّل
  `LicenseState.deviceId` عند الإقلاع.

### ✅ المرحلة 2 — دفتر الديون
- **مخطط Drift v7**: جدولا `customers` و`ledger_entries` (تراكمي بحت؛ الربط بين
  البيع والزبون يعيش في `ledger_entries.invoiceId`، تفادياً للغم العمود المكرّر
  `sales_invoices.customer_id` اليتيم).
- `features/ledger/` مع `CustomerBloc` / `LedgerBloc`، وإدارة الزبائن، و**دفتر
  مُوقَّع أحادي القيد** (مدين/دائن)، و**أرصدة مُشتقّة** (`SUM(CASE …)`، لا تُخزَّن
  أبداً)، ومبالغ مُقرَّبة إلى منزلتين عند الكتابة.
- **البيع بالأجل** موصول بالدفع (`_CreditAwareConfirm` ←
  `ConfirmSaleEvent.customerId` ← `saveInvoice(customerId:)` ← قيد مدين ضمن
  **نفس المعاملة** التي تكتب الفاتورة وتخصم المخزون).
- **حارس الحذف** (`ConflictFailure`) — لا يمكن حذف زبون له حركات؛ مع راية
  `isArchived` للإخفاء الناعم.

### ✅ تبويب الدفتر في الشريط السفلي
- `/customers` هو الفرع الرابع (خمسة تبويبات: نقاط البيع، التقارير، المنتجات،
  الزبائن، الإعدادات).

### ✅ كشف الحساب — مشاركة وطباعة حرارية
- `buildCustomerStatement` ← كشف عربي نصّي واحد، يُستخدم في الاثنين:
  - **المشاركة** (`share_plus`) — تذكيرات عبر واتساب.
  - **الطباعة** — `ReceiptImage.buildTextEscPosBytes` يرسم النص إلى **صورة
    نقطية** (العربية يجب أن تُشحن كبكسلات؛ مسار ESC/POS النصّي يطبع `?`).
- كما عولجت ثغرة كامنة من المرحلة 1: أُضيف `INTERNET` واستعلام `https` من نوع
  VIEW إلى بيان أندرويد (لم تكن إصدارات الـ release تصل إلى خادم الترخيص ولا
  تفتح واتساب).

### ✅ المرحلة 3 — فكّ القفل الفوري عبر FCM  *(صار حيّاً الآن)*

> **تحديث (2026-07-27):** حيّ و**مُتحقَّق منه على جهاز** — تأكّد وصول الإشعارات
> فعلياً من مشروع Firebase الحقيقي `fawateer-4c9bc`. وملف `google-services.json`
> في مكانه، وسطر إضافة google-services في Gradle مُثبَّت (لم يعد معلّقاً).
> ⚠️ ذلك الملف **مستثنى من Git**، لذا فإن أي نسخة جديدة من المستودع **ستفشل في
> البناء** حتى تُعيده.
- `PushNotificationService` (`features/licensing/data/services/`): رسالة بيانات
  حقلها `data.type` ضمن {`new_plan_activated`، `subscription_activated`،
  `license_updated`} ← `onLicenseChanged` ← يربطها `main.dart` بـ
  `LicenseBloc.add(CheckLicenseEvent())` ← تنقلب البوابة إلى "مفعّل" **دون إعادة
  تشغيل**.
- الرمز: `activate()` يرفق الرمز المخزّن مع `create_device`؛ و
  `LicenseRepository.registerPushToken` ← `update_my_data` عند تدوير الرمز.
- **يعطّل نفسه ذاتياً**: بلا مشروع Firebase يفشل `Firebase.initializeApp()`
  بلطف ← الإشعارات مُطفأة، والتطبيق يبنى ويعمل كما هو.
- الاعتماديات: `firebase_core ^4`، `firebase_messaging ^16`، و
  `POST_NOTIFICATIONS` في البيان.
- **خطوات التفعيل وحمولة الخادم:** [`android/README-fcm.md`](android/README-fcm.md).

### ✅ معرّف الجهاز على iOS
- صار `DeviceIdentityService` يُجزّئ `identifierForVendor` (عبر
  `device_info_plus`) على iOS، بنفس الملح المستخدم في مسار `ANDROID_ID` — فتنتج
  المنصّتان رمزين مبهمين متكافئين. ومعرّف أصلي فارغ (مثلاً على iOS قبل أول فتح
  للقفل) يعود إلى الثابت الاحتياطي؛ لا يرمي استثناءً أبداً.

### ✅ شريط الإشعار أثناء عمل التطبيق
- إشعار ترخيص يصل والتطبيق **مفتوح** صار يُظهر شريطاً مرئياً داخل التطبيق
  (`subscriptionActivatedBanner`) إضافةً إلى إعادة الفحص الصامتة. يميّز
  `PushNotificationService._handleMessage` بين التسليم في المقدمة والخلفية؛
  والمقدمة وحدها تُطلق `onForegroundLicenseChange` التي يوجّهها `main.dart` عبر
  `rootMessengerKey`. أما تسليم الخلفية فيظهر أصلاً كإشعار في شريط النظام، فلا
  يتضاعف. بلا اعتمادية جديدة. **مُتحقَّق منه على جهاز.**

### ✅ المرحلة 3.5 — الإعدادات البعيدة + فحص التحديث
- `core/config/` (`RemoteConfig`، `RemoteConfigService`): يجلب ملف إعدادات
  مستضافاً عند الإقلاع (بمهلة محدودة ← ذاكرة SharedPreferences ← القيم
  المخبوزة)، ثم يطبّقه. **وصار مستضافاً ذاتياً** على
  `https://evotech-sys.com/config/fawateer.json` — فتقاعدت استضافة Google Drive
  (`uc?export=download`) التي وصفتها هذه الخطة بالهشّة. والملف تحت إدارة الإصدارات
  في [`deploy/fawateer.json`](deploy/fawateer.json) ويُنشر منه؛ انظر
  [`deploy/README.md`](deploy/README.md).
- **عنوان الـ API وجهات الدعم وقت التشغيل**: صارت `ApiConfig.baseUrl` وواتساب
  وتيليغرام والبريد قابلة للتغيير ويقرأها `ApiClient` مع كل طلب، فيمكن نقل
  الخادم أو قنوات التواصل **دون شحن إصدار جديد**. والقيمة المخبوزة الافتراضية هي
  `https://api.evotech-sys.com/api/fawateer` — وكونها *صحيحة* هو بيت القصيد، إذ
  إنها ما يعود إليه فشل جلب الإعدادات. وحين كانت تسمّي الخادم القديم، كان أي فشل
  في الجلب يوجّه المستخدمين بصمت إلى الخادم الخطأ ويبدو كأنه نجاح.
- **نافذة التحديث داخل التطبيق**: تقارن `latest_version` من الإعدادات بالإصدار
  المثبَّت (`package_info_plus`)، وإن كان أحدث تعرض نافذة عربية لمرة واحدة مع
  `update_notes` وزر **تنزيل** يفتح رابط APK المطابق للمعمارية (عبر
  `supportedAbis` من `device_info_plus`). مغلَّفة على مستوى التطبيق بـ
  `_UpdateChecker` لتعمل مهما كانت شاشة بوابة الترخيص.
- الاعتمادية: `package_info_plus`. وملفات APK تُخدَّم من المنصّة نفسها
  (`api.evotech-sys.com/api/v1/downloads/latest/invoices/android/<abi>`)، فصار
  ملف الإعدادات والملفات التي يشير إليها على مضيف واحد.

### ✅ المرحلة 3.5 — إصلاح تجمّد شاشة البدء + تمتين البوابة
- **إصلاح جذري لتجمّد "جارٍ التحقق من الاشتراك…" الأبدي**: الانتظار غير المحدود
  الوحيد عند الإقلاع — قناة `ANDROID_ID` / `identifierForVendor` — صار محدوداً
  بمهلة (3 ثوانٍ في `DeviceIdentityService` ← يعود إلى المعرّف الثابت). الأجهزة
  الجديدة تماماً تتخطى استطلاع الشبكة وتذهب مباشرة إلى نموذج التفعيل، و`_onCheck`
  مغلَّف بحيث **لا شيء** يمكنه ترك شاشة البدء تدور (دائماً `bootstrapped: true`).
- **إعادة كتابة البوابة**: عولجت حالة كامنة كان فيها مستخدم غير مفعّل يعلق على
  `/splash`، وصار التوجيه يعتمد بوضوح على `bootstrapped` + `registered` + `isBusy`.

### ✅ المرحلة 3.5 — تعديل بيانات الحساب في الإعدادات
- اكتسبت الإعدادات قسم **بيانات الحساب** (على غرار `AccountSettingsPage` في
  Smart-Agent): **الاسم** و**الهاتف** قابلان للتعديل عبر ورقة سفلية، مع بطاقة
  **معرّف الجهاز** القابل للنسخ — كلها حيّة من `LicenseBloc` المشترك.
- `LicenseRepository.updateAgent` **يحفظ محلياً أولاً** (فلا يضيع شيء)، ثم يزامن
  مع الخادم بأفضل جهد (`update_my_data` مع `full_name`/`phone`)؛ ونتيجة
  `AgentSaveOutcome` تقود إشعاراً أخضر ("تمت المزامنة") أو برتقالياً ("حُفظ
  محلياً").

### ✅ نقاط البيع — منتقي المنتجات: قائمة حيّة وتغذية راجعة
- صارت ورقة "إضافة صنف" تقرأ قائمة المنتجات **حيّةً** من `ProductBloc` (بدل لقطة
  واحدة) — ما يعالج خطأ الظهور فارغة عند أول فتح، ويعرض مؤشر تحميل حتى أول بثّ؛
  والبحث يُفلتر أثناء الكتابة.
- كل بطاقة تعطي تأكيداً واضحاً: **تكبير سريع + وميض علامة صح خضراء** عند اللمس،
  و**شارة حيّة بعدد ما في السلة**.

### ✅ نقاط البيع — البيع بالوزن / نوع البيع  *(مخطط v8)*
- تعداد `ProductSaleType` قابل للتوسّع (`piece` | `weight`، مع متسع للحجم/الطول/
  الصندوق) على `Product`، محفوظ **بالاسم** في عمود نصّي تراكمي
  `products.saleType` (**من v7 إلى v8**، بقيمة افتراضية `'piece'` — فلا يتغيّر
  أي منتج قائم). واكتسبت نماذج الإضافة/التعديل مُنتقي نوع بيع، وتنقلب تسمية
  السعر إلى "لكل كغ".
- **إدخال البيع بالوزن**: نافذة بحقلين تعرض **الوزن (كغ)** و**المبلغ** معاً
  ومترابطين حيّاً (اكتب أحدهما فيُعاد حساب الآخر بسعر الكيلو). موصولة بالمنتقي،
  وبمسح الباركود (عبر `measuredPrompt` في الحالة كي يبقى الـ BLoC خالياً من
  الواجهة)، وبتعديل سطر السلة (اكتسب `AddProductToCartEvent` كمية مطلقة اختيارية).
- **دقّة المبلغ تماماً**: يُخزَّن الوزن بدقّة `double` كاملة (ويُقرَّب للعرض فقط)،
  فيُعيد `price × quantity` بناء المبلغ المُدخَل بالضبط (`5000` ← `5000.00`) — بلا
  حاجة لعمود يخزّن إجمالي السطر. وتعرض شاشة الدفع والفاتورة العربية النقطية
  الوزن مع وحدة `كغ`.

### ✅ نقاط البيع — مركز تدقيق المبيعات  *(إعادة صياغة تبويب السجل)*
- صار `HistoryBloc` / `HistoryPage` **مركز تدقيق مُفلتَراً ومُرقَّم الصفحات**:
  `SalesFilter` (نطاق تاريخ `today`/`yesterday`/`thisWeek`/`thisMonth`/`custom`،
  و`PaymentFilter` `all`/`cash`/`credit`، وبحث نصّي، وترتيب — الافتراضي **اليوم**)
  مع **ملخّص حيّ** (العدد / الإجمالي / إجمالي الآجل).
- التمييز بين النقد والأجل **مُشتقّ** لكل فاتورة (آجلة إذا كان لها قيد مدين في
  الدفتر). إسقاطات DAO جديدة: `AuditInvoiceRow` (مع `isCredit` و`customerName`
  و`itemCount`) و`AuditSummaryRow`، عبر `SalesDao.watchAuditInvoices` /
  `watchAuditSummary`.
- **حيّ ومُرقَّم**: القائمة والملخّص يركب كلٌّ منهما `StreamSubscription` يغذّي
  أحداثاً داخلية `_InvoicesUpdated` / `_SummaryUpdated` (لا يمكن `emit` خارج
  المعالج)؛ وتغيير الفلتر أو `LoadMoreEvent` (`_kPageSize = 30`) يُعيد الاشتراك.
  وبنود الفاتورة تُحمَّل عند الطلب وتُخزَّن لصفحة `/history/detail/:id`؛ وإعادة
  الطباعة تستخدم نفس `PrinterRepository.printReceipt`.

### ✅ الصندوق — درج النقد  *(مخطط v9)*
- ميزة جديدة `features/cashbox/` — دفتر نقدي **أحادي القيد، مُوقَّع، مُشتقّ
  الرصيد** (بنفس شكل دفتر الديون). صف واحد في `cashbox_transactions` لكل حركة؛
  والرصيد = `SUM(amount)` (بإشارة `+` للداخل و`−` للخارج)، لا يُخزَّن أبداً.
  المبالغ `double` مُقرَّبة لمنزلتين عند الكتابة. الهجرة **من v8 إلى v9** تراكمية
  (`createTable` + `_createCashboxIndexes()`)؛ لم يُمسّ أي جدول قائم.
- تعداد `CashTransactionType` قابل للتوسّع، محفوظ **بالاسم** (لا بالفهرس)، لكل
  نوع اتجاه افتراضي. والنوعان `purchasePayment` / `supplierPayment` **محجوزان**
  لوحدتَي المشتريات/المورّدين غير المبنيتَين بعد — يمكنهما القيد هنا بلا هجرة
  حين تُشحنان.
- **قيود تلقائية يملكها مصدرها**: **بيع نقدي** يقيّد دخلاً من نوع `cashSale` ضمن
  معاملة البيع (`SalesDao.insertInvoiceWithItems`؛ و**سداد دين** يقيّد دخلاً من
  نوع `customerDebtPayment` ضمن معاملة السداد (`LedgerRepositoryDriftImpl` — لذا
  صار `LedgerRepository` يعتمد أيضاً على `CashboxDao`). كلاهما مرتبط عبر
  `relatedId`، ومُعلَّم `isSystemGenerated` (لا يحذفه المستخدم)، و**يُعكَس** عند
  حذف الفاتورة أو القيد المصدر (`CashboxDao.deleteByRelatedId`) كي يبقى الرصيد
  صادقاً.
- يُوصل إليه من **الإعدادات ← الصندوق** (`/settings/cashbox` ← `/history`)، لا
  عبر تبويب جديد (تبقى الواجهة بخمسة تبويبات). و`CashboxBloc` على مستوى التطبيق
  (`LoadCashbox` عند الإقلاع). ويشترك مع الدفتر في تنسيق
  `core/utils/money_display.dart`.

### ✅ الدفتر — منع تكرار اسم الزبون
- إضافة أو تعديل زبون باسم يستخدمه زبون آخر صار يعيد `Left(DuplicateFailure)`
  (نوع خطأ جديد)، مربوطاً بـ `CustomerMessage.duplicateName` ← النص المُترجَم
  `duplicateCustomerName`. كما يتحقق النموذج مباشرةً عبر
  `CustomersDao.nameExists(exceptId:)`.

### ✅ البناء — إصلاح بناء الإصدار على أندرويد
- كان `flutter build apk` يفشل على `flutter_vibrate` (المتوقّفة): تربط مواردها
  بإصدار SDK أقدم من 31 ← `android:attr/lStar not found`. صار
  `android/build.gradle.kts` يرفع `compileSdkVersion` لأي مشروع فرعي قديم إلى 34
  عبر كتلة `subprojects { afterEvaluate { … } }` مُسجَّلة **قبل** كتلة
  `evaluationDependsOn(":app")` (لا يمكنك `afterEvaluate` مشروعاً قُيّم أصلاً).
  تم التحقق: `--split-per-abi` ينتج الملفات الثلاثة.

---

### ✅ الخطة 001 — النسخ الاحتياطي والاستعادة  *(Google Drive)*
- تنسخ **ملف SQLite الحيّ بأكمله** (`VACUUM INTO`)، لا تصديراً للصفوف أو JSON —
  فتُلتقط كل الجداول بلا تسلسل خاص لكل ميزة، لكن الاستعادة **كل شيء أو لا شيء**.
  نطاق `drive.file` فقط، اختير تحديداً كي لا تطلب Google مراجعة توثيق للتطبيق.
  لا تشفير من جهة العميل.
- **ثلاثة حرّاس مستقلّة للاستعادة**: رفض النسخة الأحدث مخططاً، وفحص SHA-256
  *قبل* المساس بقاعدة البيانات الحيّة، وتبديل قابل للتراجع (نسخة `.pre-restore`،
  مع حذف `-wal`/`-shm` أولاً). والاستعادة **تُنهي التطبيق** — لا إعادة تهيئة
  لـ `AppDatabase`/`sl` داخل Dart.
- يعمل `AutoBackupService` عند الإقلاع/الاستئناف، ويتخطى إن مضى أقل من 24 ساعة
  (بلا WorkManager). ويُسجَّل حساب Drive على الخادم ويُلمَّح به عند إعادة التثبيت.
  بلا جدول جديد.

### ✅ الخطة 003 — العملة المزدوجة  *(مخطط v10)*
- الليرة السورية هي **عملة الدفاتر الوحيدة**؛ والدولار مجرد *ملصق تسعير* على
  المنتج، يُحوَّل إلى ليرة لحظة البيع. صمدت القاعدة: لم يحتج أي شيء لاحق (السجل،
  الدفتر، الصندوق، التقارير) إلى تغيير.
- سعر الصرف يعيش في سطرين من `AppSettings`، **لا في جدول**. و`usdToSp` يُقرِّب
  إلى ليرة كاملة ويعيد `null` حين لا يكون مضبوطاً، فيُجبَر المستدعي على الحماية —
  و`CartItem.isUnpriced` يمنع إتمام البيع. والفواتير القديمة محصّنة ضد تعديل
  السعر لاحقاً (`priceCurrency`/`fxRate`/`priceOriginal` مُلتقطة لكل سطر).

### ✅ الخطة 005 — الخصومات اليدوية  *(مخطط v12)*
- خصم لكل سطر + خصم واحد على كامل السلة، وكلاهما مخزَّن **كمبلغ محسوم بالليرة**.
  الاختيار بين النسبة والمبلغ شأن واجهة فقط — **لا تُخزَّن راية `isPercentage`**.
  وخصم السلة **يُضاف فوق** خصومات السطور.
- التقييد (clamping) مكرَّر عمداً في ثلاثة مواضع؛ وهذا حامل للحمل لأن الخصومات
  لا يُعاد التحقق منها عند تغيّر الكمية.

### ✅ الخطة 006 — الفترة التجريبية المجانية
- راية عرض واحدة `isTrial` — **بلا نقطة نهاية جديدة، وبلا حالة `LicenseStatus`
  جديدة، وبلا هجرة**، و**بلا أي تفريع للتجربة داخل `LicenseGuards`**. فالبوابة
  والمهلة وفحص التلاعب تعامل التجريبي والمدفوع بالتساوي.
- منع الاستغلال **من جهة الخادم فقط**، مرتكزاً على معرّف الجهاز. أما تخزين تاريخ
  أول تشغيل محلياً فمرفوض صراحةً (يُصفَّر بمسح بيانات التطبيق).
- تغيّرت الحرّاس منذئذ: مهلة العمل دون إنترنت **7 أيام** (وشريط تحذير من اليوم
  الثالث)، وعتبة التلاعب بالساعة **48 ساعة**. والترخيص الصالح المحجوب بحارس
  يُوجَّه إلى `/activation/verify`، لا إلى صفحة الباقات.

### ✅ الخطة 007 — المشاركة / واتساب  *(بطاقات PNG)*
- ناقل واحد (`share_plus`) + مُصيّرات قابلة للتركيب، فيعمل واتساب وتيليغرام
  والبريد وDrive **بلا شيفرة خاصة لكل قناة**.
- `captureWidgetToPng` يركّب البطاقة خارج الشاشة داخل `Overlay` *الحقيقي*
  للتطبيق — وهذا متعمَّد كي ترث `Directionality`/`Localizations`/`Theme`، وهو ما
  يجعل العربية وRTL تُرسم صحيحة. **وهي ليست نفس تقنية الفواتير الحرارية**
  (رسم أحادي اللون بعرض 384 بكسل عبر `dart:ui`)؛ ولا تُعمَّم إحداهما على الأخرى.

### ✅ الخطة 008 — لوحة التحليلات  *(بلا جداول جديدة)*
- `DashboardDao` هو `@DriftAccessor` للقراءة فقط فوق ستة جداول قائمة؛ وكل تجميعة
  تُحسب **في SQL**، لا بجمع الصفوف في Dart.
- ليست مساراً ولا تبويباً — بل العرض الافتراضي *داخل* تبويب التقارير.
- حيّة عبر **مؤشّر تغيّر** (`SELECT 1` مع `readsFrom:`)، فأي كتابة بيع/نقد/دين/
  مخزون تُعيد تشغيل التجميعات العشر كلها.
- ⚠️ صيغة SQL للربح **مكرَّرة عمداً** مع `SalesDao.watchAuditSummary` — غيّر
  إحداهما وغيّر الأخرى وإلا اختلفت الشاشتان.

### ✅ الخطة 010 — خصائص المنتجات الديناميكية  *(السلة A مكتملة، مخطط v13)*
- حقول مخصّصة يعرّفها صاحب المحل: التعريفات في `attribute_definitions`، والقيم
  لكل منتج خريطة JSON. **بلا جدول فهرسة** — البحث والفلترة يجريان في Dart فوق
  قائمة المنتجات في الذاكرة (أُسقط الجدول عمداً من أجل المحال البسيطة).
- **v1.1** طباعة الحقول المُعلَّمة `showOnReceipt` (مُلتقطة وقت البيع، محصّنة عند
  إعادة الطباعة). **v1.2** بحث بأي قيمة خاصية + رقاقات فلترة. **v1.3** تقرير
  "المبيعات حسب الحقل" عبر `json_extract` في SQL (مسار JSON **معامل مربوط**).
  **v1.4** ملصقات منتجات حرارية بـ Code128/QR.
- **غير مبنيّ (خطط منفصلة):** السلة B (تنويعات المقاس×اللون). *(السلة C شُحنت
  كالخطة 012.)*

### ✅ الخطة 011 — إصلاحات ملاحظات الميدان  *(الأحد عشر بنداً، مخطط v14)*
المصدر: ملاحظات صاحب محل بعد استخدام حقيقي. شُحنت على أربع موجات — **أ** (حقول
إضافة منتج فارغة افتراضياً، الصنف الممسوح يقفز للأعلى، ترتيب المنتجات بالأحدث،
إشعارات أسرع)؛ **ب** (إظهار نفاد المخزون، تخطيط الفاتورة كجدول من ستة أعمدة)؛
**ج** (حجم الخط على مستوى التطبيق، مفتاح زر الطباعة)؛ **د** (إصلاح شامل للماسح).
- **قصة الماسح هي الأبرز.** منتج "لا يمكن قراءته" تبيّن أنه **باركود معكوس** —
  أعمدة بيضاء على علبة حمراء — وهو ما لا يستطيع ML Kit فكّه أصلاً بأي زاوية.
  والتطبيق المنافس يستخدم ML Kit أيضاً، فالمحرّك لم يكن الفرق قط؛ بل
  **المعالجة المسبقة للإطار**. عولج بترقية `mobile_scanner` من 5.2.3 إلى 7.4.0
  للحصول على `invertImage` وشحن مفتاح "باركود فاتح". **تم تأكيد القراءة على
  الجهاز.**
- أُضيف أيضاً: قائمة صيغ مسموحة `formats:`، ودقّة تحليل 1280×720 مع **تراجع
  ذاتي الإصلاح** (كان التثبيت الصلب على 1920×1080 قد أوقع "الكاميرا غير متاحة")،
  و**تأكيد متعدد الإطارات** (قراءتان متطابقتان متتاليتان) لرفض القراءات الخاطئة
  رغم صحّة الـ checksum، إضافة إلى لمس-للتركيز حقيقي وتكبير.
- **التبديل التلقائي للقطبية دُرس ورُفض**: `invertImage` يُحدَّد وقت الإنشاء،
  فكل قلب يُعيد ربط الكاميرا (~0.3–0.6 ثانية عمياء). فشُحنت بدلاً منه رقاقة
  تلميح بعد 6 ثوانٍ بلا قراءة — فالمفتاح كان يعمل أصلاً، لكن لا شيء كان يخبر
  الكاشير *متى* يمدّ يده إليه.
- **متابعة:** لقطة `sales_items.saleType` (v13→v14) — واكتُشف أثناء تحديد
  نطاقها أن **إعادة الطباعة كانت تُسقط الوحدة تماماً** (الأصل ≠ إعادة طباعة
  الفاتورة نفسها). تم التحقق على جهاز عبر
  `integration_test/migration_v14_test.dart`.

### ✅ الخطة 012 — القطع المُسلسَلة  *(IMEI/الرقم التسلسلي، مخطط v15)*
- المنتج (`Product`) هو صنف؛ أما `ProductUnit` فهو قطعة مادية واحدة. خمسة هواتف
  متطابقة = **صف منتج واحد وخمسة صفوف قطع** — وهو النموذج الذي حمته الخطة 010
  حين رفضت جعل IMEI خاصيةً على مستوى الصنف.
- الاشتراك اختياري لكل منتج (`isSerialized`)، فالمحل الذي لا يفعّله يرى سلوك
  اليوم بالضبط.
- **مسار مسح ثانٍ**: الباركود أولاً ثم الرقم التسلسلي — فمسح IMEI يختار ذلك
  الجهاز بعينه. والجهاز المباع يُبلَّغ عنه بـ **unitNotAvailable**، لا "المنتج
  غير موجود" عمداً، إذ إن الأخيرة سترسل الكاشير يفتّش الرفّ عن هاتف بيع الأسبوع
  الماضي.
- سطر واحد لكل جهاز، لا كمية 2 أبداً — فالرقم التسلسلي مُلتقط لكل سطر، والدمج
  سيُضيع IMEI بالكامل.
- `sales_items.serialSnapshot` يتبع قاعدة "إعادة الطباعة أبدية"؛ ويظهر الرقم على
  الفاتورة، وعلى **إعادة طباعتها**، وفي صفحة تفاصيل الفاتورة.
- **الواجهة لا تقول "IMEI" أبداً** — تقول *الرقم التسلسلي* و*قطعة*. فهذا نظام
  نقاط بيع **عام**؛ الهواتف حفّزت الخطة لكن محال العدّة والأجهزة والمجوهرات
  تستخدم الميزة نفسها، وكان المصطلح يوحي لها بأنها ليست لها.
- ⚠️ **الميزة الوحيدة غير المُتحقَّق منها ميدانياً** — شُحنت بعد الجولة التي
  أثبتت كل ما عداها. واختبار هجرة v14→v15 مكتوب لكنه لم يُشغَّل. والخطر محدود:
  الهجرة تراكمية بحتة والميزة اختيارية لكل منتج.

### ✅ التنقّل — زر الرجوع في أندرويد
- كان الرجوع من جذر أي تبويب يتصاعد إلى النظام و**يُغلق التطبيق فوراً** (أبلغ
  محلٌّ أن لمسة إبهام طائشة أغلقت الصندوق في منتصف عملية بيع). صار `AppShell`
  يحمل `PopScope(canPop: false)`: من تبويب غير نقاط البيع يعود إلى الرئيسية، وفي
  نقاط البيع يحتاج ضغطة ثانية مؤكِّدة.
- وهذا آمن لأن Flutter يسأل **أعمق مُتنقِّل أولاً** — فالصفحة المدفوعة تُغلق على
  مُتنقِّل فرعها ولا تصل إلى القشرة أصلاً.

---

## مؤجّل بقرار — *وليس* قائمة انتظار

هذه قرارات مغلقة، لا أعمال ناقصة. وإعادة فتح أي منها ينبغي أن تبدأ من وثيقة
الخطة، فهي تسجّل السبب.

- ⏸️ **الخطة 002 — المزامنة متعددة الأجهزة** (الإصدار الثاني). أرض أنظمة موزّعة
  حقيقية؛ والخطة مُقسَّمة لتشحن النصف الخالي من التعارضات أولاً.
- ⏸️ **الخطة 009 — المساعد الذكي والتنبيهات** (الإصدار الثاني). الخطة 008 تشحن
  أصلاً النسخة *السلبية* من أهم ثلاثة تنبيهات (قائمة المخزون المنخفض، قائمة أكبر
  المدينين، أسهم فروق المؤشرات)؛ و009 تحوّلها من سحب إلى دفع، وهو تلميع لشيء
  موجود لا قدرة مفقودة.
- ❌ **الخطة 004 — إعادة احتساب الأسعار التاريخية. مرفوضة نهائياً.** البيعة
  المكتملة واقعة منتهية. لا تبنِ هذا.
- 🔒 **الخطة 005** — سعر تخفيض دائم لكل منتج (v1.5) ومحرّك القواعد التلقائي
  (الإصدار الثاني).
- 🔒 **الخطة 007** — مُصيّر PDF (اعتمادية `pdf` ما تزال غير مُضافة عمداً).
- 🔒 **الخطة 008 v1.5+** — المبيعات بالساعة، دوران المخزون، المنتجات بطيئة
  الحركة، أكثر الزبائن شراءً، مشاركة اللوحة كصورة.
- 🔒 **الخطة 010** — السلة B (تنويعات المقاس×اللون). *(السلة C شُحنت كالخطة 012.)*
- ⬜ **وحدات أكبر** — فواتير المشتريات، المورّدون، المصاريف، تعدد الفروع. أُبقيت
  المعمارية قابلة للتوسّع لأجلها؛ و`CashTransactionType` يحجز أصلاً
  `purchasePayment`/`supplierPayment` كي تُقيَّد في الصندوق بلا هجرة.

---

## التحقّق الميداني — ما الذي يعمل في محل حقيقي

**كان هذا القسم هو العائق. لم يعد كذلك.** فواتير في استخدام إنتاجي يومي داخل
محل، وقد تحقّق المالك من كل ميزة على هاتفه فور شحنها — مُبلِّغاً عن أي خلل
مباشرةً، ومن هناك جاءت الخطة 011.

والعلاقة بين طبقتَي الاختبار تستحق التصريح، لأن أياً منهما لا يُغني عن الأخرى:
يُشغّل `flutter test` عدد **127 اختباراً** على **بدائل مكتوبة يدوياً**
(و`flutter analyze` نظيف)، ولا يلمس Drift ولا SQLite الأصلية ولا الطابعة ولا
الكاميرا ولا الشبكة — وذلك بالتصميم. فهو يُثبت المنطق؛ أما الجولة على الجهاز
فتُثبت التطبيق.

**ما تأكّد على جهاز حقيقي:**
- 🟢 **استعادة النسخة الاحتياطية من Google Drive** — من الطرف للطرف، ضد نسخة
  حقيقية. كان هذا أخطر مسار في التطبيق (يحذف قاعدة البيانات الحيّة ويستبدلها)
  وآخر ما بقي دون اختبار.
- 🟢 **الصندوق** — القيد التلقائي، وعكسه، وحسابات الرصيد المُشتقّ.
- 🟢 **إشعارات Firebase** — وصول فعلي من مشروع Firebase الحقيقي.
- 🟢 **المنتجات المُباعة بالوزن** — نافذة الحقلين، ووحدة `كغ` على الفاتورة.
- 🟢 **زر الرجوع في أندرويد** — تأكّد أن إصلاح `PopScope` (`48c6fb2`) لم يعد
  يُغلق التطبيق من جذر تبويب.
- 🟢 **التشغيل اليومي للمحل** — نقاط البيع، وسجل المبيعات، والفواتير المطبوعة،
  وبوابة الترخيص، مُجرَّبة باستمرار من نشاط تجاري حقيقي لا بجولة اختبار مُعدّة.
- 🟢 الهجرة **v13→v14** (`integration_test/migration_v14_test.dart`)، وصيغة SQL
  لتقرير **المبيعات حسب الحقل** (`integration_test/sales_by_field_test.dart`)،
  وإصلاح **الباركود المعكوس** على المنتج الأحمر الذي كان يفشل فعلاً.

**الثغرة الوحيدة المتبقية:**
- 🟠 **الخطة 012 — القطع المُسلسَلة** (مخطط **v14→v15**). شُحنت بعد جولة التحقّق
  أعلاه، فلم تعمل لا الميزة ولا هجرتها على جهاز. الاختبار
  `integration_test/migration_v15_test.dart` مكتوب وجاهز، ويحتاج تشغيلة واحدة
  على جهاز موصول. والخطر حقيقي لكنه محدود — فالهجرة تراكمية بحتة
  (`addColumn` ×2 + `createTable`)، والميزة اختيارية لكل منتج، فالمحل الذي لا
  يفعّلها لا يتأثر في الحالتين.

**التوصية:** شغّل اختبار هجرة v15 على جهاز قبل تفعيل الخطة 012 في محل حقيقي.
وكل ما عداها مُثبَت ميدانياً.

### بنود تشغيلية — أُنجزت

- ✅ **فواتير يعمل على خادمه الخاص.** `api.evotech-sys.com/api/fawateer` (منصّة
  EVOTECH، `evotech-core`)، بديلاً عن خادم Smart-Agent المؤقت. ولأن المنصّة
  بُنيت على عقد الحمولات القائم، اقتصر التغيير في التطبيق على العنوان الأساسي
  ولا شيء غيره.
- ✅ **الإعدادات البعيدة مستضافة ذاتياً** على
  `evotech-sys.com/config/fawateer.json`، خارج Google Drive. فزالت الهشاشة التي
  حذّرت منها الخطة 001 — أن تُغيّر Drive نظام روابط التنزيل فتفقد قدرتك على
  تحويل الخادم دون إصدار جديد. ومصدر الحقيقة هو `deploy/fawateer.json` في هذا
  المستودع.
- ✅ **ملفات APK تُخدَّم من المنصّة نفسها**، فصارت نافذة التحديث والملفات التي
  تشير إليها على مضيف واحد.

### بنود تشغيلية ما تزال قائمة

- ✅ **مفتاح توقيع الإصدار مُؤمَّن ومُتحقَّق منه** (2026-07-27) — ثلاث نسخ من
  ملف `.jks` (الحاسوب، وGoogle Drive، وقرص خارجي)، وكلمات المرور في Bitwarden
  مُزامَنة على الهاتف والحاسوب. والتحقّق تمّ من الطرف للطرف لا مجرد تخزين:
  `keytool` يؤكّد تطابق بصمة SHA-1 مع Google Cloud Console، وبناء إصدار كامل
  وُقِّع بنجاح — وهو الشيء الوحيد الذي يُثبت صحّة كلمتَي المرور معاً. ومع ذلك
  يبقى فقدانه غير قابل للاسترداد ويبقى يعزل كل التثبيتات، فتعامل مع ترتيب
  النسخ في `docs/android-release-signing.md` كأمر يجب إبقاؤه صحيحاً، لا كمهمة
  انتهت.
  - 🟠 يبقى إجراءان هناك: تمتين حساب Bitwarden (تفعيل التحقق بخطوتين + حفظ رمز
    الاسترداد خارج الإنترنت — فالخزنة لا تملك إعادة تعيين لكلمة المرور)، ثم حذف
    ملف كلمات المرور النصّي الموجود حالياً بجانب ملف `.jks` على Drive.
- 🟠 **`google-services.json` مستثنى من Git**، فأي نسخة جديدة من المستودع
  **تفشل في بناء أندرويد** حتى تُعيده (إضافة Gradle مُطبَّقة دون شرط). انظر
  `android/README-fcm.md`.

---

## سجل الـ commits (هذا الجهد)

| الـ commit | ماذا |
|--------|------|
| `681bcbc` | بوابة الترخيص + دفتر الديون |
| `589c4d2` | كشف الحساب (مشاركة) + إصلاحات شبكة أندرويد |
| `9a8e10f` | شاشة إدارة الاشتراك |
| `8d843b7` | معرّف جهاز قابل للنسخ |
| `d51d465` | تبويب الزبائن في الشريط السفلي |
| `8a4c6a6` | كشف حساب مطبوع حرارياً |
| `18edcf1` | فكّ القفل الفوري بـ FCM (خامل افتراضياً) |
| `3ff2fe8` | إضافة ROADMAP.md لتتبّع التقدّم التجاري |
| `728c342` | معرّف جهاز iOS عبر `identifierForVendor` |
| `d71ab8f` | شريط داخل التطبيق عند فكّ قفل الاشتراك حيّاً |
| `b05c357` | الإعدادات البعيدة + فحص التحديث + إصلاح تجمّد شاشة البدء |
| `9927e82` | منتقي المنتجات — قائمة حيّة وتغذية راجعة |
| `abba223` | نوع البيع: بيع المنتجات بالوزن/المبلغ (مخطط v8) |
| `f02617d` | تعديل اسم/هاتف الحساب في الإعدادات (+ نصوص ترجمة) |
| `1c2d765` | توثيق: البيع بالوزن، الإعدادات البعيدة، تعديل الحساب، المنتقي |
| `4f2a774` | جدول حركات الصندوق + الترجمة (مخطط v9) |
| `9bf5378` | نموذج حركة النقد + المستودع |
| `0c69c32` | ترجمة اختيار/إضافة زبون |
| `b9e41b8` | التحقق من تكرار اسم الزبون + الترجمة |
| `3e1e961` | سجل المبيعات ومركز التدقيق |
| `f6b5e82` | إصلاح بناء الإصدار على أندرويد (`flutter_vibrate` / compileSdk 34) |

### منذ 2026-07-11 (مجمّعة — 50 commit)

| الـ commits | ماذا |
|--------|------|
| `933300f`, `a3bea68`, `34dbe87` | **الخطة 001** — نسخ/استعادة Drive، تلميح الحساب عند إعادة التثبيت، مفتاح النسخ التلقائي |
| `6571bac` | مراجعة معمارية الخطة 002 (تصميم فقط، ما تزال للإصدار الثاني) |
| `66d74d5`, `e788adf`, `1f06a4b` | **الخطة 003** — العملة المزدوجة، `ل.س` افتراضياً، نافذة سعر الصرف |
| `166cb1b` | **الخطة 005** — خصومات يدوية للسطر ولكامل السلة |
| `895a6c4`, `7b69b35`, `ac33f7d`, `f337daf` | **الخطة 006** — الفترة التجريبية، إشعارات الانتهاء، شاشة التحقق + شريط عدم الاتصال |
| `169ad46` | **الخطة 007** — بطاقات مشاركة PNG مُنسّقة |
| `15debe0` | **الخطة 008** — لوحة التحليلات |
| `8f4b4dd` | تأجيل الخطة 009 للإصدار الثاني مع المبررات |
| `92ddd57`, `a20c1ef`, `a9fcb5c`, `4ad8e3a`, `86af86b`, `1b4c289` | **الخطة 010** — الخصائص الديناميكية v1 ← v1.4 (الحقول، الطباعة، البحث/الفلترة، تجميع التقارير، الملصقات) |
| `159496b`, `b19d83c`, `a006531`, `481df9c`, `7523f5d`, `fcb5380` | **الخطة 011** — الموجات أ–د |
| `f03e8cf`, `e210349`, `5256c79`, `b8c31f6`, `41068db` | تمتين الماسح — تراجع الدقّة، تأكيد متعدد الإطارات، mobile_scanner 7.4.0 + مفتاح العكس، رقاقة التلميح |
| `854ccb7` | لقطة `sales_items.saleType` (**مخطط v13→v14**) |
| `48c6fb2` | زر الرجوع في أندرويد لم يعد يغلق التطبيق من جذر تبويب |
| `ebc1fbe` | وضع المخزون الصارم (منع البيع تحت الصفر — اختياري) |
| `84d1992` | توقيع الإصدار بمفتاح مملوك + ملفات APK لكل معمارية |
| `069e49a`, `9c0c325`, `00e4966` | الهوية — Cairo مضمّن دون إنترنت، أدوار أزرار مُنسَّقة، أيقونة الشريط-ف التكيّفية |
| `1d9c14c`, `9aafc22`, `487991a`, `b3c7e15`, `0305b19` | معالجة FCM في المقدمة، تمتين فحص التحديث ونافذته، روابط تنزيل لكل معمارية، إعدادات الـ API |
| `ca7c2ad`, `606a171`, `4c86dd2`, `60f2332`, `f808cf9` | تفرّد المنتجات وبحث الباركود غير الموجود، إغلاق الإشعارات، تلميع الإعدادات والدعم |
| `dc388fd`, `1778abc` | أعمال ترتيب — ملفات متعقَّبة شاردة، تصحيح انحراف CLAUDE.md |
| `0f1f855` | توثيق: تحديث ROADMAP وترويسات الخطط الخمس القديمة |
| `514d3b6` | إصلاح: استعادة تفشل بعد إغلاق قاعدة البيانات يجب أن تطلب إعادة تشغيل |
| `8083d03`, `d452d67`, `baa2f52` | **الخطة 012** — القطع المُسلسَلة (مخطط v15)، واجهة القطع ومسار المسح الثاني، وإزالة "IMEI" من الواجهة |
