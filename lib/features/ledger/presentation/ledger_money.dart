// Money formatting helpers (`currencyOf`, `moneyText`). The implementation now
// lives in `core/utils/money_display.dart` and is shared with the cashbox
// feature; this file re-exports it so existing ledger imports keep working.
export '../../../core/utils/money_display.dart';
